<%= form_for micropost do |f| %>
  <%= render "shared/error_messages", object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: t("new_microposts") %>
  </div>
  <%= f.submit t("create"), class: "btn btn-primary" %>
<% end %>
